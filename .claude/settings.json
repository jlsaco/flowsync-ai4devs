{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/frontend/*) cd \"$CLAUDE_PROJECT_DIR/frontend\" && npx --no-install prettier --write --ignore-unknown \"$f\" >/dev/null 2>&1 ;; esac; true",
            "timeout": 30,
            "statusMessage": "Formateando con Prettier (frontend)…"
          }
        ]
      }
    ]
  }
}
